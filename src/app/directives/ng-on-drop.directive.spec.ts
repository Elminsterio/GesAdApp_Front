import { NgOnDropDirective } from './ng-on-drop.directive';

describe('NgOnDropDirective', () => {
  it('should create an instance', () => {
    const directive = new NgOnDropDirective();
    expect(directive).toBeTruthy();
  });
});
